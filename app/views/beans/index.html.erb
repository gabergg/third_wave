<h1>Check Deez Beans Homie!!!!!!!!</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Summary</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

  <% @beans.each do |bean| %>
      <tr>
        <td><%= bean.name %></td>
        <td><%= bean.roaster %></td>
        <td><%= bean.avg_rating %></td>
        <td><%= link_to "Rate", new_review_path(:bean_id => bean.id) %></td>
        <td><%= link_to "Remove", bean, method: :delete, data: { confirm: "Are you sure?" } %></td>
      </tr>
  <% end %>
</table>

<br>

<%= link_to "Add a bean", new_bean_path %>